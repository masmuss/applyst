name: release-image

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run semantic-release in dry-run mode (no tag/release/image push)'
        required: true
        default: true
        type: boolean
      platforms:
        description: 'Comma-separated target platforms, for example linux/amd64,linux/arm64'
        required: false
        default: ''
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write
  packages: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  DEFAULT_IMAGE_PLATFORMS: 'linux/amd64,linux/arm64'

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      IS_DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run == true }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Prepare image name
        id: image
        run: echo "name=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Resolve image platforms
        id: platforms
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_PLATFORMS: ${{ inputs.platforms }}
          DEFAULT_PLATFORMS: ${{ env.DEFAULT_IMAGE_PLATFORMS }}
        run: |
          platforms="$DEFAULT_PLATFORMS"

          if [ "$EVENT_NAME" = "workflow_dispatch" ] && [ -n "$INPUT_PLATFORMS" ]; then
            platforms="$INPUT_PLATFORMS"
          fi

          if [ -z "$platforms" ]; then
            platforms="linux/amd64,linux/arm64"
          fi

          echo "value=$platforms" >> "$GITHUB_OUTPUT"

      - name: Semantic Release
        id: semantic
        uses: cycjimmy/semantic-release-action@v6
        with:
          semantic_version: 24
          dry_run: ${{ env.IS_DRY_RUN }}
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
            conventional-changelog-conventionalcommits
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Docker Buildx
        if: steps.semantic.outputs.new_release_published == 'true'
        uses: docker/setup-buildx-action@v4

      - name: Setup QEMU
        if: steps.semantic.outputs.new_release_published == 'true'
        uses: docker/setup-qemu-action@v4

      - name: Login to GHCR
        if: steps.semantic.outputs.new_release_published == 'true'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: steps.semantic.outputs.new_release_published == 'true'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ steps.platforms.outputs.value }}
          push: true
          tags: |
            ${{ steps.image.outputs.name }}:${{ steps.semantic.outputs.new_release_version }}
            ${{ steps.image.outputs.name }}:latest
